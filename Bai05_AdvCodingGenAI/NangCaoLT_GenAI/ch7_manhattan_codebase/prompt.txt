CONTEXT: you are provided with:
1. A Python function enclosed with {{{ FUNCTION }}}
2. Explanation points enclosed with {{{ HEADERS }}}
TASK: Explain the function using the explanation points.
FUNCTION: {{{
import numpy as np
import pandas as pd


def get_manhattan_distance(
    df1: pd.DataFrame,
    df2: pd.DataFrame,
) -> np.float64:
    """
    Tính Manhattan distance (L1 norm) giữa hai DataFrame cùng kích thước.
    """
    if not isinstance(df1, pd.DataFrame) or not isinstance(df2, pd.DataFrame):
        raise TypeError("df1 và df2 phải là pandas.DataFrame")

    if df1.shape != df2.shape:
        raise ValueError("df1 và df2 phải cùng shape.")

    if list(df1.columns) != list(df2.columns) or list(df1.index) != list(df2.index):
        raise ValueError("df1 và df2 phải cùng index và columns.")

    try:
        df1_num = df1.astype(float)
        df2_num = df2.astype(float)
    except ValueError as err:
        raise ValueError("Giá trị trong DataFrame phải có thể chuyển thành số") from err

    element_wise_dist: pd.DataFrame = (df1_num - df2_num).abs()
    dist: np.float64 = np.array(element_wise_dist).sum().astype(float)
    return np.float64(dist)
}}}
HEADERS: {{{
1. Function purpose
2. Arguments and their types
3. Step-by-step data flow
4. Output and its type
5. Potential edge cases
}}}
EXPLANATION:
1. Function purpose:
   - `get_manhattan_distance` tính khoảng cách Manhattan (L1 norm) giữa hai `pandas.DataFrame` đồng kích thước với cùng chỉ số và cột.
   - Kết quả là tổng giá trị tuyệt đối của các hiệu phần tử tương ứng.

2. Arguments and their types:
   - `df1`: `pd.DataFrame` đầu tiên.
   - `df2`: `pd.DataFrame` thứ hai.
   - Trả về: `np.float64`.

3. Step-by-step data flow:
   - Kiểm tra kiểu: nếu `df1` hoặc `df2` không phải DataFrame thì ném `TypeError`.
   - Kiểm tra `shape`: nếu không giống thì ném `ValueError`.
   - Kiểm tra `index` và `columns`: nếu khác nhau thì ném `ValueError`.
   - Chuyển hai DataFrame sang `float` bằng `.astype(float)`.
     * Nếu có giá trị không thể chuyển thì ném `ValueError` với thông điệp lỗi rõ ràng.
   - Tính hiệu tuyệt đối: `(df1_num - df2_num).abs()` tạo DataFrame chênh lệch.
   - Tổng hai lần: `np.array(...).sum()` tính tổng mọi phần tử.
   - Quyết định kiểu kết quả: ép sang `np.float64`.

4. Output and its type:
   - Thu được một giá trị số thực `np.float64`, biểu diễn Manhattan distance tổng của tất cả phần tử.

5. Potential edge cases:
   - `df1` hoặc `df2` không phải `pd.DataFrame`.
   - Kích thước khác nhau (`shape` khác).
   - `index` hoặc `columns` khác nhau.
   - Dữ liệu không thể chuyển sang số thực (chuỗi không numeric, giá trị NaN/inf tùy vào cách pandas xử lý).
   - DataFrame rỗng (`shape` = (0,0)) trả về 0.0.
